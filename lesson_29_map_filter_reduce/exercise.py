
# Lesson 29: The `map()`, `filter()`, and `reduce()` Functions

# Instructions:

# 1. Use `map()` to convert a list of strings to a list of integers.
# 2. Use `filter()` to get all the numbers from a list that are greater than 10.
# 3. From the `functools` module, import `reduce`. Use it to find the product of all numbers in a list.

# Your code goes here:
