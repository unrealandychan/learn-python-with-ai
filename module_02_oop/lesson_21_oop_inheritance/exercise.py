
# Lesson 21: OOP: Inheritance

# Instructions:

# 1. Create a `Vehicle` class with `make` and `model` attributes, and a `display_info` method.
# 2. Create a `Car` class that inherits from `Vehicle`. It should have an additional `number_of_doors` attribute.
# 3. Create an `ElectricCar` class that inherits from `Car`. It should have a `battery_capacity` attribute.
# 4. Create objects for each class and call their `display_info` methods.

# Your code goes here:
