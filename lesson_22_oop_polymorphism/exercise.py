
# Lesson 22: OOP: Polymorphism & Method Overriding

# Instructions:

# 1. Create a base class `Shape` with a method `area()` that returns 0.
# 2. Create a `Rectangle` class that inherits from `Shape` and overrides `area()` to return the correct area.
# 3. Create a `Circle` class that inherits from `Shape` and overrides `area()` to return the correct area.
# 4. Create a list containing a `Rectangle` and a `Circle` object.
# 5. Loop through the list and print the area of each shape.

# Your code goes here:
